function main()
{
	def number a;
	define number b;
    define number c;
    define number max = 0;

    cin<< a;

    if ( a < b ) { max = b; }
    else { max = a; }

    if ( c > max ) { c = max; }

    cout>> max;
}